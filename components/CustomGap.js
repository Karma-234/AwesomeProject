import { View, Text } from 'react-native'
import React from 'react'

const CustomGap = ({height, width}) => {
  return (
    <View style={{height: height, width: width}}>
    </View>
  )
}

export default CustomGap;